### Summary
- Исправлено отображение даты и времени в истории для всех статусов уведомлений.

### Changes
- Теперь при нажатии кнопок `ok` и `cancel` в историю записывается не только статус, но и текущая дата и время.
- Улучшена логика парсинга истории: теперь плагин корректно определяет статус уведомления, даже если формат записи в истории изменился.
- При создании нового уведомления через виджет теперь корректно вставляется выбранный эмодзи.

### Verification
- Проект успешно собран (`node esbuild.config.mjs production`).
- Код метода `updateNotificationStatus` гарантирует добавление временной метки к каждой новой записи в истории.
- Логика `parseBlock` обновлена для поддержки нового формата записей.